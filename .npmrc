# Relax strict peer dependencies to allow React 19 to coexist with Radix UI
strict-peer-dependencies=false
auto-install-peers=true

# Next.js / TypeScript hoisting optimizations
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*@types*

# Prevent TTY buffer truncation in Vercel logs
reporter=append-only