[pytest]
# Adds the root directory to the python path so 'api' can be imported
pythonpath = .

# Automatically treat all async functions as async tests
asyncio_mode = auto

# Optional: suppress third-party warnings for cleaner output
filterwarnings =
    ignore::DeprecationWarning